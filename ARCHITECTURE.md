# Application Architecture

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                            │
│                    http://localhost:5173                        │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ HTTP Requests
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    REACT FRONTEND (Vite)                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Pages                                                   │   │
│  │  ├── Home.jsx        (Landing page)                     │   │
│  │  ├── About.jsx       (Company info)                     │   │
│  │  ├── Blog.jsx        (Blog listing)                     │   │
│  │  └── Contact.jsx     (Contact form) ◄── API Integration│   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Components                                              │   │
│  │  ├── Navbar.jsx      (Navigation)                       │   │
│  │  └── Footer.jsx      (Footer)                           │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ fetch('/api/contacts', {...})
                          │ CORS Enabled
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│              NODE.JS BACKEND - Express Server                   │
│                    http://localhost:5000                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  server.js                                               │   │
│  │  ├── CORS Middleware                                     │   │
│  │  ├── JSON Parser                                         │   │
│  │  ├── Request Logger                                      │   │
│  │  └── Error Handler                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Routes                                                  │   │
│  │  ├── /api/health      → Health Check                    │   │
│  │  ├── /api/contacts    → Contact Routes                  │   │
│  │  └── /api/blog        → Blog Routes                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Controllers                                             │   │
│  │  ├── contactController.js                               │   │
│  │  │   ├── createContact()                                │   │
│  │  │   ├── getContacts()                                  │   │
│  │  │   ├── updateContactStatus()                          │   │
│  │  │   └── deleteContact()                                │   │
│  │  └── blogController.js                                  │   │
│  │      ├── getBlogPosts()                                 │   │
│  │      ├── getBlogPostBySlug()                            │   │
│  │      └── createBlogPost()                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Validation (express-validator)                         │   │
│  │  ├── Email validation                                    │   │
│  │  ├── String length checks                               │   │
│  │  └── Required field checks                              │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ SQL Queries (Parameterized)
                          │ pg.Pool Connection
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    POSTGRESQL DATABASE                          │
│                     salesman_tracking                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Tables                                                  │   │
│  │                                                          │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  contacts                                        │   │   │
│  │  │  ├── id (serial PK)                              │   │   │
│  │  │  ├── name (varchar 255)                          │   │   │
│  │  │  ├── email (varchar 255)                         │   │   │
│  │  │  ├── company (varchar 255)                       │   │   │
│  │  │  ├── message (text)                              │   │   │
│  │  │  ├── created_at (timestamp)                      │   │   │
│  │  │  └── status (varchar 50)                         │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                                                          │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  blog_posts                                      │   │   │
│  │  │  ├── id (serial PK)                              │   │   │
│  │  │  ├── title (varchar 500)                         │   │   │
│  │  │  ├── slug (varchar 500, unique)                  │   │   │
│  │  │  ├── excerpt (text)                              │   │   │
│  │  │  ├── content (text)                              │   │   │
│  │  │  ├── author (varchar 255)                        │   │   │
│  │  │  ├── published (boolean)                         │   │   │
│  │  │  ├── created_at (timestamp)                      │   │   │
│  │  │  └── updated_at (timestamp)                      │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                                                          │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  subscribers                                     │   │   │
│  │  │  ├── id (serial PK)                              │   │   │
│  │  │  ├── email (varchar 255, unique)                 │   │   │
│  │  │  ├── subscribed_at (timestamp)                   │   │   │
│  │  │  └── active (boolean)                            │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow: Contact Form Submission

```
┌──────────────┐
│ 1. User      │  User fills contact form and clicks "Book Demo"
│    Action    │
└──────┬───────┘
       │
       ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. Frontend    Contact.jsx                                     │
│    (React)     ├── Validates input                             │
│                ├── Shows loading state                          │
│                └── fetch('http://localhost:5000/api/contacts') │
│                    {                                            │
│                      method: 'POST',                            │
│                      body: JSON.stringify(formData)             │
│                    }                                            │
└───────┬────────────────────────────────────────────────────────┘
        │
        │ HTTP POST Request
        │ Content-Type: application/json
        │
        ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. Backend     Express Server                                  │
│    Middleware  ├── CORS Check ✓                                │
│                ├── Parse JSON Body ✓                            │
│                └── Log Request ✓                                │
└───────┬────────────────────────────────────────────────────────┘
        │
        ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. Router      /api/contacts (POST)                            │
│                └── Apply validation middleware                  │
│                    ├── name: required, 2-255 chars              │
│                    ├── email: required, valid format            │
│                    ├── company: optional, max 255 chars         │
│                    └── message: required, 10-5000 chars         │
└───────┬────────────────────────────────────────────────────────┘
        │
        ▼
┌────────────────────────────────────────────────────────────────┐
│ 5. Controller  contactController.createContact()               │
│                ├── Check validation results                     │
│                ├── If errors → return 400                       │
│                └── If valid → proceed to database               │
└───────┬────────────────────────────────────────────────────────┘
        │
        │ SQL Query (Parameterized)
        │ INSERT INTO contacts (name, email, company, message)
        │ VALUES ($1, $2, $3, $4) RETURNING *
        │
        ▼
┌────────────────────────────────────────────────────────────────┐
│ 6. Database    PostgreSQL                                      │
│                ├── Execute INSERT                               │
│                ├── Create new row in contacts table             │
│                └── Return inserted row with id                  │
└───────┬────────────────────────────────────────────────────────┘
        │
        │ Success! Row inserted
        │
        ▼
┌────────────────────────────────────────────────────────────────┐
│ 7. Response    {                                               │
│                  "success": true,                               │
│                  "message": "Contact form submitted...",        │
│                  "data": {                                      │
│                    "id": 1,                                     │
│                    "name": "John Doe",                          │
│                    "email": "john@example.com",                 │
│                    "company": "Acme Corp",                      │
│                    "message": "I'd like a demo",                │
│                    "created_at": "2026-01-30T...",              │
│                    "status": "new"                              │
│                  }                                              │
│                }                                                │
└───────┬────────────────────────────────────────────────────────┘
        │
        │ HTTP 201 Created
        │
        ▼
┌────────────────────────────────────────────────────────────────┐
│ 8. Frontend    Contact.jsx receives response                   │
│    Update      ├── setLoading(false)                           │
│                ├── setSubmitted(true)                           │
│                └── Show success message ✅                      │
│                                                                 │
│                "Thank you for contacting us!"                   │
│                "We'll get back to you within 24 hours."         │
└───────┬────────────────────────────────────────────────────────┘
        │
        │ After 3 seconds
        │
        ▼
┌────────────────────────────────────────────────────────────────┐
│ 9. Reset       ├── Clear form fields                           │
│    Form        ├── setSubmitted(false)                         │
│                └── Ready for next submission                    │
└────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow

```
┌──────────────────────┐
│   User Input         │
│  Name: John Doe      │
│  Email: john@ex.com  │
│  Company: Acme       │
│  Message: Demo req   │
└──────────┬───────────┘
           │
           ▼
    React State
    (formData)
           │
           ▼
    JSON.stringify
           │
           ▼
┌──────────────────────┐
│  HTTP Request Body   │
│  {                   │
│    "name": "John",   │
│    "email": "...",   │
│    "company": "...", │
│    "message": "..."  │
│  }                   │
└──────────┬───────────┘
           │
           ▼
    Express Middleware
    (JSON Parser)
           │
           ▼
    req.body Object
           │
           ▼
┌──────────────────────┐
│   Validation         │
│  ✓ Name length       │
│  ✓ Email format      │
│  ✓ Message length    │
└──────────┬───────────┘
           │
           ▼
    SQL Parameters
    [$1, $2, $3, $4]
           │
           ▼
┌──────────────────────┐
│  PostgreSQL Table    │
│  contacts            │
│  ┌────────────────┐  │
│  │ Row Inserted   │  │
│  │ ID: 1          │  │
│  │ Status: new    │  │
│  │ Created: now   │  │
│  └────────────────┘  │
└──────────┬───────────┘
           │
           ▼
    Database Response
    (Inserted Row)
           │
           ▼
┌──────────────────────┐
│  JSON Response       │
│  {                   │
│    "success": true,  │
│    "data": {...}     │
│  }                   │
└──────────┬───────────┘
           │
           ▼
    React State Update
           │
           ▼
┌──────────────────────┐
│   UI Update          │
│  ✅ Success Message  │
│  Form Reset          │
└──────────────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────┐
│ Layer 1: Input Validation (Frontend)       │
│ ├── Required fields checked                 │
│ ├── Email format validated                  │
│ └── Character limits enforced               │
└──────────┬──────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Layer 2: CORS Protection                    │
│ ├── Only localhost:5173 allowed             │
│ └── Other origins rejected                  │
└──────────┬──────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Layer 3: Server-Side Validation             │
│ ├── express-validator checks                │
│ ├── Strict type checking                    │
│ ├── Length limits enforced                  │
│ └── Email normalization                     │
└──────────┬──────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Layer 4: SQL Injection Protection           │
│ ├── Parameterized queries ($1, $2, $3)      │
│ ├── No string concatenation                 │
│ └── PostgreSQL prepared statements          │
└──────────┬──────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Layer 5: Error Handling                     │
│ ├── Try-catch blocks                        │
│ ├── Sanitized error messages                │
│ └── No sensitive data exposed               │
└─────────────────────────────────────────────┘
```

---

## 📁 File Relationships

```
Contact.jsx
    │
    ├─► Imports React hooks (useState, useEffect)
    │
    ├─► Reads VITE_API_URL from .env
    │
    └─► Makes fetch() request to:
            │
            ▼
        server.js
            │
            ├─► Loads config from backend/.env
            │
            ├─► Initializes database.js
            │   └─► Creates PostgreSQL connection pool
            │
            └─► Routes request to:
                    │
                    ▼
                contactRoutes.js
                    │
                    ├─► Applies validation middleware
                    │
                    └─► Calls controller:
                            │
                            ▼
                        contactController.js
                            │
                            └─► Executes SQL via database.js
                                    │
                                    ▼
                                PostgreSQL
                                (salesman_tracking)
```

---

## 🌐 Port Configuration

```
Development Environment:

┌──────────────────────────────┐
│  Frontend (Vite)             │
│  Port: 5173                  │
│  .env: VITE_API_URL          │
└──────────────────────────────┘

┌──────────────────────────────┐
│  Backend (Express)           │
│  Port: 5000                  │
│  backend/.env: PORT          │
└──────────────────────────────┘

┌──────────────────────────────┐
│  PostgreSQL Database         │
│  Port: 5432                  │
│  backend/.env: DB_PORT       │
└──────────────────────────────┘

All running on localhost (127.0.0.1)
```

---

## 🔄 State Management Flow

```
Contact Form Component State:

formData: {
  name: '',
  email: '',
  company: '',
  message: ''
}
    ↓
  User types
    ↓
handleChange()
    ↓
formData updated
    ↓
User clicks submit
    ↓
handleSubmit()
    ↓
setLoading(true)
    ↓
fetch API call
    ↓
  ┌─── Success ─────┐
  │                 │
  ▼                 ▼
setSubmitted(true)  setError(msg)
setLoading(false)   setLoading(false)
Show ✅ message     Show ❌ error
  │
  │ After 3 seconds
  ▼
Reset form
setSubmitted(false)
```

---

This architecture ensures:
- ✅ Separation of concerns
- ✅ Scalability
- ✅ Security
- ✅ Maintainability
- ✅ Clear data flow
- ✅ Error handling at every layer
